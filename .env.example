# By default the app calls Safaricom Identity API (fetchMaskedMsisdn) to verify Safaricom data.
# Optional override: your own API that returns { "allowed": false } or 403 when not on Safaricom.
# VITE_CHECK_CARRIER_URL=https://your-api.com/check-carrier
